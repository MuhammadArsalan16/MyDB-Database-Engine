[metadata]
name = mydb-client
version = 0.1.0
description = Pure-Python client for the MyDB database engine (mydbd)
license = MIT

[options]
packages = mydb_client
python_requires = >=2.7
zip_safe = True
